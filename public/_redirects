# Netlify redirects for Next.js with @netlify/plugin-nextjs
# The plugin handles most routing automatically

# Preserve Next.js static assets
/_next/static/* /_next/static/:splat 200

# Handle API routes (plugin manages these)
/api/* /.netlify/functions/__nextjs_handler 200

# Fallback for client-side routing (handled by plugin)
/* /index.html 200
